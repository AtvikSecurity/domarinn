// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { GrantLevel } from "./GrantLevel";
import type { RunId } from "./RunId";

/**
 * `GET /sets/{project}/suites/{suite}` response. The same aggregates as the
 * suite's row in the project detail, addressed directly.
 */
export type SuiteSetDetailResponse = { project: string, suite: string, restricted: boolean, my_level: GrantLevel | null, run_count: number, 
/**
 * Epoch-ms.
 */
last_run_at: number | null, pass_count: number, fail_count: number, error_count: number, case_count: number, 
/**
 * Empty-output cases across the set's runs — see this module's header.
 */
empty_count?: number, latest_pass_rate: number | null, 
/**
 * The last 20 runs' pass rates, oldest first — see `SuiteSetView`.
 */
sparkline: Array<number>, baseline_run_id: RunId | null, 
/**
 * The pinned baseline branch, when the suite's baseline is a branch pin
 * rather than a fixed run. Mutually exclusive with `baseline_run_id`.
 */
baseline_branch: string | null, };
