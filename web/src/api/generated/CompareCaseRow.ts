// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { AssertFlip } from "./AssertFlip";
import type { CaseChange } from "./CaseChange";
import type { CaseKey } from "./CaseKey";
import type { CaseStatus } from "./CaseStatus";
import type { CompareDelta } from "./CompareDelta";

/**
 * One case's row in a compare response.
 */
export type CompareCaseRow = { case_key: CaseKey, name: string | null, base_status: CaseStatus | null, head_status: CaseStatus | null, delta: CompareDelta, output_changed: boolean, 
/**
 * The case's overall score in the base run (column-only; `None` when the
 * case is absent from base or the row carries no score).
 */
base_score: number | null, 
/**
 * The case's overall score in the head run.
 */
head_score: number | null, 
/**
 * `head_score - base_score`, only when *both* are present.
 */
score_delta: number | null, 
/**
 * Assertions whose pass/fail flipped between base and head for this case;
 * empty when nothing flipped or the case exists on only one side. See
 * [`AssertFlip`] for the (necessarily heuristic) pairing.
 */
assert_flips: Array<AssertFlip>, 
/**
 * *What* moved for this case, as opposed to whether the verdict did.
 * `unknown` whenever either side predates component digests — that is the
 * honest answer, and it is common against historical runs because
 * `provider_digest` can never be backfilled.
 */
change: CaseChange, };
