{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/AtvikSecurity/domarinn/main/domarinn.schema.json",
  "title": "Suite",
  "description": "The top-level eval suite.",
  "type": "object",
  "properties": {
    "version": {
      "description": "Config schema version. Currently always `1`.",
      "type": "integer",
      "format": "uint32",
      "minimum": 0
    },
    "project": {
      "description": "Project namespace (groups runs on the server).",
      "type": [
        "string",
        "null"
      ]
    },
    "suite": {
      "description": "Suite name (names the run's suite).",
      "type": [
        "string",
        "null"
      ]
    },
    "description": {
      "type": [
        "string",
        "null"
      ]
    },
    "extends": {
      "description": "A base suite to deep-merge on top of (composition).",
      "type": [
        "string",
        "null"
      ]
    },
    "imports": {
      "description": "Reusable fragments (named assert-sets, shared providers) to import.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "providers": {
      "description": "The systems under test. At least one is required.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/Provider"
      }
    },
    "prompts": {
      "description": "Optional prompts. Omit when a provider constructs its own input.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/Prompt"
      }
    },
    "tools": {
      "description": "Tools every provider in this suite may offer the model.\n\nDeclaring a tool does not make domarinn run one — it never executes a\ntool and never feeds a result back. What it wants is the model's\n*decision*, reported as `tool_calls` and graded by `tool-call`\nassertions. Suite-level rather than per-test because the tool surface is\na property of the system being evaluated, and varying it per case would\nmake two cases incomparable while looking like the same suite.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/ToolDef"
      }
    },
    "tests": {
      "description": "Test sources: inline cases, `file://` globs, or generator commands.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/TestSource"
      },
      "default": []
    },
    "defaults": {
      "description": "Values merged into every test.",
      "anyOf": [
        {
          "$ref": "#/$defs/Defaults"
        },
        {
          "type": "null"
        }
      ]
    },
    "grader": {
      "description": "Default grader for `llm-rubric` assertions.",
      "anyOf": [
        {
          "$ref": "#/$defs/Grader"
        },
        {
          "type": "null"
        }
      ]
    },
    "runner": {
      "anyOf": [
        {
          "$ref": "#/$defs/Runner"
        },
        {
          "type": "null"
        }
      ]
    },
    "cache": {
      "anyOf": [
        {
          "$ref": "#/$defs/CacheCfg"
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "additionalProperties": false,
  "required": [
    "version",
    "providers"
  ],
  "$defs": {
    "Provider": {
      "description": "A system under test.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        },
        "label": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "id"
      ],
      "oneOf": [
        {
          "description": "Spawn an external command speaking the exec JSON protocol.",
          "type": "object",
          "properties": {
            "command": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "env": {
              "type": "object",
              "additionalProperties": {
                "type": "string"
              }
            },
            "timeout_ms": {
              "type": [
                "integer",
                "null"
              ],
              "format": "uint64",
              "minimum": 0
            },
            "cache_salt": {
              "description": "Extra cache-busting token (e.g. a git SHA or binary hash).\n\nNot normally needed: every `command` argument naming a readable file\ncontributes its path and a digest of its contents to the cache key,\nso an edit or a rebuild busts the entry on its own. Set this when the\nprogram's behavior depends on something that identity cannot see — a\nmodel downloaded at startup, a remote config, a container image\nbehind a wrapper script.",
              "type": [
                "string",
                "null"
              ]
            },
            "program_identity": {
              "description": "Whether the program's own identity enters the cache key. Default\n`true`; set `false` to make `cache_salt` the *sole* identity.\n\nContent digests are reproducible across machines, so the default is\nright for a checked-in script. It is not right for a binary compiled\nin CI: Rust builds are not byte-reproducible (embedded paths, debug\ninfo), so two runners building identical source produce different\nbytes and therefore different keys, and the shared cache never hits.\nOnly the suite knows those two builds are the same version — so it\nsays so, by pinning `cache_salt` to a digest of the *source* and\nturning this off.\n\nRequires `cache_salt`: with neither an identity nor a salt the key\nwould be argv alone, which does not move when the program is\nrebuilt, so responses are not cached at all.",
              "type": "boolean"
            },
            "type": {
              "type": "string",
              "const": "exec"
            }
          },
          "required": [
            "type",
            "command"
          ]
        },
        {
          "description": "Native Anthropic Messages API client.",
          "type": "object",
          "properties": {
            "model": {
              "type": "string"
            },
            "base_url": {
              "type": [
                "string",
                "null"
              ]
            },
            "api_key_env": {
              "anyOf": [
                {
                  "$ref": "#/$defs/EnvNames"
                },
                {
                  "type": "null"
                }
              ]
            },
            "params": {
              "type": [
                "object",
                "null"
              ],
              "additionalProperties": true
            },
            "pricing": {
              "description": "Rate override for this provider's model. See [`PricingCfg`].\n\nBoxed because `ProviderKind` is reachable from `AssertKind` (an\n`llm-rubric` can carry its own grader, which carries a provider), so\nfive inline `Option<f64>` here would inflate every assertion in\nevery suite by 80 bytes. Transparent to serde and schemars — the\nYAML is unchanged.",
              "anyOf": [
                {
                  "$ref": "#/$defs/PricingCfg"
                },
                {
                  "type": "null"
                }
              ]
            },
            "type": {
              "type": "string",
              "const": "anthropic"
            }
          },
          "required": [
            "type",
            "model"
          ]
        },
        {
          "description": "OpenAI-compatible chat-completions client.",
          "type": "object",
          "properties": {
            "model": {
              "type": "string"
            },
            "base_url": {
              "type": [
                "string",
                "null"
              ]
            },
            "api_key_env": {
              "anyOf": [
                {
                  "$ref": "#/$defs/EnvNames"
                },
                {
                  "type": "null"
                }
              ]
            },
            "params": {
              "type": [
                "object",
                "null"
              ],
              "additionalProperties": true
            },
            "pricing": {
              "description": "Rate override for this provider's model. See [`PricingCfg`].\n\nBoxed because `ProviderKind` is reachable from `AssertKind` (an\n`llm-rubric` can carry its own grader, which carries a provider), so\nfive inline `Option<f64>` here would inflate every assertion in\nevery suite by 80 bytes. Transparent to serde and schemars — the\nYAML is unchanged.",
              "anyOf": [
                {
                  "$ref": "#/$defs/PricingCfg"
                },
                {
                  "type": "null"
                }
              ]
            },
            "type": {
              "type": "string",
              "const": "openai"
            }
          },
          "required": [
            "type",
            "model"
          ]
        },
        {
          "description": "Arbitrary HTTP endpoint.",
          "type": "object",
          "properties": {
            "url": {
              "type": "string"
            },
            "method": {
              "anyOf": [
                {
                  "$ref": "#/$defs/HttpMethod"
                },
                {
                  "type": "null"
                }
              ]
            },
            "headers": {
              "type": "object",
              "additionalProperties": {
                "type": "string"
              }
            },
            "body": true,
            "output_expr": {
              "description": "minijinja expression selecting the output from the response.",
              "type": [
                "string",
                "null"
              ]
            },
            "type": {
              "type": "string",
              "const": "http"
            }
          },
          "required": [
            "type",
            "url"
          ]
        },
        {
          "description": "Embeddings endpoint used by the `similar` assertion.",
          "type": "object",
          "properties": {
            "model": {
              "type": "string"
            },
            "base_url": {
              "type": [
                "string",
                "null"
              ]
            },
            "api_key_env": {
              "anyOf": [
                {
                  "$ref": "#/$defs/EnvNames"
                },
                {
                  "type": "null"
                }
              ]
            },
            "params": {
              "type": [
                "object",
                "null"
              ],
              "additionalProperties": true
            },
            "pricing": {
              "description": "Rate override. Only `input_per_mtok` is read: an embedding call has\nno output tokens and reports no cache counters, so the other fields\nwould price components that do not exist.",
              "anyOf": [
                {
                  "$ref": "#/$defs/PricingCfg"
                },
                {
                  "type": "null"
                }
              ]
            },
            "type": {
              "type": "string",
              "const": "embeddings"
            }
          },
          "required": [
            "type",
            "model"
          ]
        }
      ]
    },
    "EnvNames": {
      "description": "One or more environment variable names holding an API key.\n\nA bare string is the single-name form and serializes back to a bare string,\nso every existing suite's `config_digest` is unchanged and no `--against`\ncomparison shows spurious drift. Pinned by\n`a_single_env_name_serializes_as_a_bare_string`.\n\nThe first name that resolves to a non-empty value wins. Which one that was\nis logged at debug rather than stored: the run document is shareable, and a\nvariable name is a weak but real signal about someone's environment. That\ndoes mean two environments resolving different names produce runs that\n`config_digest` cannot tell apart — check the log if that matters.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      ]
    },
    "PricingCfg": {
      "description": "A per-provider rate override, in USD per million tokens.\n\nMerged field-wise over the built-in rate for the provider's model, so a\nsuite can correct one stale number without restating a whole price sheet.\nExists for the cases a shipped table cannot cover: a proxy or gateway with\nnegotiated rates, a fine-tune, or a model the table has never heard of.\n\n`f64` is right *here* and nowhere else — a human writes `3.00` in YAML, and\nit converts to integer micro-dollars once when the provider is built.\n\nNever reaches a provider's `fingerprint()`, so setting it does not\ninvalidate a single cache entry: cost is not request identity.",
      "type": "object",
      "properties": {
        "input_per_mtok": {
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "output_per_mtok": {
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "cache_read_per_mtok": {
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "cache_write_per_mtok": {
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "cache_write_1h_per_mtok": {
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        }
      },
      "additionalProperties": false
    },
    "HttpMethod": {
      "description": "The HTTP method for a `type: http` provider. Authors may write either case\n(`get` or `GET`) in YAML; the wire method (and the request/cache\nfingerprint) is always the uppercase form.",
      "type": "string",
      "enum": [
        "GET",
        "POST",
        "PUT",
        "PATCH",
        "DELETE",
        "HEAD"
      ]
    },
    "Prompt": {
      "description": "A prompt template. Exactly one of `template` / `messages` must be set.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        },
        "template": {
          "type": [
            "string",
            "null"
          ]
        },
        "messages": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "$ref": "#/$defs/Message"
          }
        }
      },
      "additionalProperties": false,
      "required": [
        "id"
      ]
    },
    "Message": {
      "description": "A chat message; `content` may be `file://path` to load from disk.",
      "type": "object",
      "properties": {
        "role": {
          "$ref": "#/$defs/ChatRole"
        },
        "content": {
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "role",
        "content"
      ]
    },
    "ChatRole": {
      "description": "A chat message role.",
      "type": "string",
      "enum": [
        "system",
        "user",
        "assistant"
      ]
    },
    "ToolDef": {
      "description": "A tool the model may call.\n\nAnthropic's field names: `input_schema` says what the value is (a JSON\nSchema) where OpenAI's `parameters` does not, and one shape had to win. The\nmapping to the other vendor is mechanical and lives in `openai.rs`.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "description": {
          "type": [
            "string",
            "null"
          ]
        },
        "input_schema": {
          "description": "JSON Schema for the tool's arguments. Passed to the provider verbatim\nand never rendered — it is a contract, not a per-case value."
        }
      },
      "additionalProperties": false,
      "required": [
        "name"
      ]
    },
    "TestSource": {
      "description": "An entry in the `tests:` list.\n\n`Deserialize` is hand-written (rather than derived as `#[serde(untagged)]`)\nso a typo inside an inline test case surfaces the precise unknown-field\nerror from [`TestCase`]'s `deny_unknown_fields` instead of the opaque\n\"data did not match any variant of untagged enum\" an untagged derive emits.\n`Serialize`/`JsonSchema` keep the untagged shape (a glob string, a\n`{generator: ...}` mapping, or an inline case mapping).",
      "anyOf": [
        {
          "description": "A `file://glob` reference to YAML/JSON/CSV/JSONL test files.",
          "type": "string"
        },
        {
          "description": "A generator command that emits test cases as JSON.",
          "$ref": "#/$defs/GeneratorWrap"
        },
        {
          "description": "An inline test case.",
          "$ref": "#/$defs/TestCase"
        }
      ]
    },
    "GeneratorWrap": {
      "type": "object",
      "properties": {
        "generator": {
          "$ref": "#/$defs/GeneratorSpec"
        }
      },
      "additionalProperties": false,
      "required": [
        "generator"
      ]
    },
    "GeneratorSpec": {
      "type": "object",
      "properties": {
        "command": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "config": true,
        "timeout_ms": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        }
      },
      "additionalProperties": false,
      "required": [
        "command"
      ]
    },
    "TestCase": {
      "description": "A single test case.",
      "type": "object",
      "properties": {
        "id": {
          "type": [
            "string",
            "null"
          ]
        },
        "tags": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "description": {
          "type": [
            "string",
            "null"
          ]
        },
        "vars": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/Val"
          }
        },
        "matrix": {
          "description": "Parameter sweep: each axis maps a var name to the list of values it takes.\nThe case fans out over the cartesian product of its axes (one case per\ncombination), with each axis value merged into `vars`. See\n[`crate::matrix`].",
          "type": "object",
          "additionalProperties": {
            "type": "array",
            "items": {
              "$ref": "#/$defs/Val"
            }
          }
        },
        "matrix_id": {
          "description": "Optional minijinja template for a matrix cell's id, rendered against the\naxis values (e.g. `\"{{ style }}-{{ temperature }}\"`). Defaults to\n`<base-id>[key=value,…]`.",
          "type": [
            "string",
            "null"
          ]
        },
        "assert": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Assert"
          }
        },
        "threshold": {
          "description": "If set, the case passes when its weighted-mean score >= threshold;\notherwise it passes only if every assert passes.",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "cache_salt": {
          "description": "Opaque per-case cache-busting token, folded into this case's provider\ncache key. Use it when the system under test loads content domarinn\ncannot see (its own prompt files), so editing that content busts only\nthe cases that use it. Never sent to the provider.\n\nThis keys *one case*, and is a different lever from a provider's own\n`cache_salt`, which pins the program behind a command. It is not a\nprerequisite for either: an `exec` provider is cached by default, keyed\non the identity of every `command` argument that names a readable file.\n\nUsed **verbatim**, and deliberately not templated: a useful salt is a\ndigest of something domarinn cannot see, so it could only be derived from\nthe environment — and `env` is deliberately kept out of the request\nidentity (see [`crate::render`]) so that unrelated environment drift\nnever busts a shared cache. Keeping it a literal also means it can never\nfail to render and is never a template-injection surface. Compute the\ndigest outside the suite and write the value in.",
          "type": [
            "string",
            "null"
          ]
        },
        "only_providers": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "skip_providers": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "Val": true,
    "Assert": {
      "description": "A single assertion with its common controls. `type: not-<kind>` is sugar for\n`negate: true` and works in every test source.",
      "type": "object",
      "properties": {
        "weight": {
          "type": "number",
          "format": "double",
          "default": 1.0
        },
        "negate": {
          "type": "boolean",
          "default": false
        }
      },
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "value": {
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "contains"
            }
          },
          "required": [
            "type",
            "value"
          ]
        },
        {
          "type": "object",
          "properties": {
            "value": {
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "icontains"
            }
          },
          "required": [
            "type",
            "value"
          ]
        },
        {
          "type": "object",
          "properties": {
            "values": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "type": {
              "type": "string",
              "const": "icontains-any"
            }
          },
          "required": [
            "type",
            "values"
          ]
        },
        {
          "type": "object",
          "properties": {
            "value": {
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "regex"
            }
          },
          "required": [
            "type",
            "value"
          ]
        },
        {
          "type": "object",
          "properties": {
            "value": {
              "$ref": "#/$defs/Val"
            },
            "type": {
              "type": "string",
              "const": "equals"
            }
          },
          "required": [
            "type",
            "value"
          ]
        },
        {
          "type": "object",
          "properties": {
            "value": {
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "starts-with"
            }
          },
          "required": [
            "type",
            "value"
          ]
        },
        {
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "is-json"
            }
          },
          "required": [
            "type"
          ]
        },
        {
          "type": "object",
          "properties": {
            "schema": {
              "anyOf": [
                {
                  "$ref": "#/$defs/Val"
                },
                {
                  "type": "null"
                }
              ]
            },
            "type": {
              "type": "string",
              "const": "contains-json"
            }
          },
          "required": [
            "type"
          ]
        },
        {
          "type": "object",
          "properties": {
            "min": {
              "type": [
                "integer",
                "null"
              ],
              "format": "uint64",
              "minimum": 0
            },
            "max": {
              "type": [
                "integer",
                "null"
              ],
              "format": "uint64",
              "minimum": 0
            },
            "type": {
              "type": "string",
              "const": "length"
            }
          },
          "required": [
            "type"
          ]
        },
        {
          "type": "object",
          "properties": {
            "value": {
              "type": "string"
            },
            "type": {
              "type": "string",
              "const": "jinja"
            }
          },
          "required": [
            "type",
            "value"
          ]
        },
        {
          "type": "object",
          "properties": {
            "command": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "config": true,
            "cache_salt": {
              "description": "Extra cache-busting token for this assertion's verdicts.\n\nSame rule as an `exec` *provider*'s: verdicts are cached by default,\nkeyed partly on the identity of every `command` argument that names a\nreadable file, so a rebuilt grader busts its own entries. Set this\nwhen the grader's behavior depends on something that identity cannot\nsee.",
              "type": [
                "string",
                "null"
              ]
            },
            "type": {
              "type": "string",
              "const": "exec"
            }
          },
          "required": [
            "type",
            "command"
          ]
        },
        {
          "description": "Assert that the model decided to call a tool.\n\nA *decision*, never an execution: domarinn does not run tools. This\ngrades what the model chose to do, which for a whole class of cases is\nthe only correct answer there is — before this, such a case produced no\nprose, scored zero against every text assertion, and read as a model\nfailure rather than an evaluation that could not see the answer.\n\nCombine with `negate` (or the `not-tool-call` sugar) for the equally\nimportant negative: the model must *not* have called `delete_account`.",
          "type": "object",
          "properties": {
            "name": {
              "description": "The tool that must have been called.",
              "type": "string"
            },
            "args": {
              "description": "Argument values that must all be present and equal. A subset match,\nnot an equality check — an assertion should not have to restate\nevery argument to pin the one that matters.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Val"
                },
                {
                  "type": "null"
                }
              ]
            },
            "schema": {
              "description": "A JSON Schema the call's arguments must satisfy. Not rendered, for\nthe same reason `contains-json`'s is not: a schema is a contract,\nnot a per-case value.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Val"
                },
                {
                  "type": "null"
                }
              ]
            },
            "type": {
              "type": "string",
              "const": "tool-call"
            }
          },
          "required": [
            "type",
            "name"
          ]
        },
        {
          "type": "object",
          "properties": {
            "value": {
              "type": "string"
            },
            "grader": {
              "description": "Boxed because it is by far the largest thing an assertion can carry\n(a whole `Grader`, including a `ProviderKind`), and every other\n`AssertKind` variant pays for it inline otherwise. Transparent to\nserde and schemars — the YAML is unchanged.",
              "anyOf": [
                {
                  "$ref": "#/$defs/Grader"
                },
                {
                  "type": "null"
                }
              ]
            },
            "threshold": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "params": {
              "type": [
                "object",
                "null"
              ],
              "additionalProperties": true
            },
            "type": {
              "type": "string",
              "const": "llm-rubric"
            }
          },
          "required": [
            "type",
            "value"
          ]
        },
        {
          "description": "Total cost in USD must be <= max. Bypasses the cache.",
          "type": "object",
          "properties": {
            "max": {
              "type": "number",
              "format": "double"
            },
            "type": {
              "type": "string",
              "const": "cost"
            }
          },
          "required": [
            "type",
            "max"
          ]
        },
        {
          "description": "Latency in ms must be <= max. Bypasses the cache.",
          "type": "object",
          "properties": {
            "max": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "type": {
              "type": "string",
              "const": "latency"
            }
          },
          "required": [
            "type",
            "max"
          ]
        },
        {
          "description": "Token count must be <= max. See [`TokenCount`] for which tokens.",
          "type": "object",
          "properties": {
            "max": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            },
            "count": {
              "anyOf": [
                {
                  "$ref": "#/$defs/TokenCount"
                },
                {
                  "type": "null"
                }
              ]
            },
            "type": {
              "type": "string",
              "const": "tokens"
            }
          },
          "required": [
            "type",
            "max"
          ]
        },
        {
          "description": "Embedding cosine similarity to a reference must be >= threshold.",
          "type": "object",
          "properties": {
            "value": {
              "$ref": "#/$defs/Val"
            },
            "threshold": {
              "type": [
                "number",
                "null"
              ],
              "format": "double"
            },
            "type": {
              "type": "string",
              "const": "similar"
            }
          },
          "required": [
            "type",
            "value"
          ]
        }
      ]
    },
    "Grader": {
      "description": "The LLM grader for `llm-rubric`.",
      "type": "object",
      "properties": {
        "provider": {
          "description": "The grading model. Should be a different model family than the SUT.",
          "$ref": "#/$defs/ProviderKind"
        },
        "template": {
          "description": "Optional override of the built-in grading-prompt template (`file://`).",
          "type": [
            "string",
            "null"
          ]
        },
        "verdict_mode": {
          "description": "`forced` (default) or `auto` — how the structured verdict is obtained.",
          "anyOf": [
            {
              "$ref": "#/$defs/VerdictMode"
            },
            {
              "type": "null"
            }
          ]
        },
        "timeout_ms": {
          "description": "How long to wait for a verdict, in milliseconds. Defaults to 120s.\n\nReachable from config because the ceiling interacts with the grader's\nown `max_tokens`: a reasoning grader given room to think can take\nlonger than a fixed constant allows, and the failure looks like a\ntransport fault rather than a budget one.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        }
      },
      "additionalProperties": false,
      "required": [
        "provider"
      ]
    },
    "ProviderKind": {
      "description": "The behavior of a provider, selected by `type`.",
      "oneOf": [
        {
          "description": "Spawn an external command speaking the exec JSON protocol.",
          "type": "object",
          "properties": {
            "command": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "env": {
              "type": "object",
              "additionalProperties": {
                "type": "string"
              }
            },
            "timeout_ms": {
              "type": [
                "integer",
                "null"
              ],
              "format": "uint64",
              "minimum": 0
            },
            "cache_salt": {
              "description": "Extra cache-busting token (e.g. a git SHA or binary hash).\n\nNot normally needed: every `command` argument naming a readable file\ncontributes its path and a digest of its contents to the cache key,\nso an edit or a rebuild busts the entry on its own. Set this when the\nprogram's behavior depends on something that identity cannot see — a\nmodel downloaded at startup, a remote config, a container image\nbehind a wrapper script.",
              "type": [
                "string",
                "null"
              ]
            },
            "program_identity": {
              "description": "Whether the program's own identity enters the cache key. Default\n`true`; set `false` to make `cache_salt` the *sole* identity.\n\nContent digests are reproducible across machines, so the default is\nright for a checked-in script. It is not right for a binary compiled\nin CI: Rust builds are not byte-reproducible (embedded paths, debug\ninfo), so two runners building identical source produce different\nbytes and therefore different keys, and the shared cache never hits.\nOnly the suite knows those two builds are the same version — so it\nsays so, by pinning `cache_salt` to a digest of the *source* and\nturning this off.\n\nRequires `cache_salt`: with neither an identity nor a salt the key\nwould be argv alone, which does not move when the program is\nrebuilt, so responses are not cached at all.",
              "type": "boolean"
            },
            "type": {
              "type": "string",
              "const": "exec"
            }
          },
          "required": [
            "type",
            "command"
          ]
        },
        {
          "description": "Native Anthropic Messages API client.",
          "type": "object",
          "properties": {
            "model": {
              "type": "string"
            },
            "base_url": {
              "type": [
                "string",
                "null"
              ]
            },
            "api_key_env": {
              "anyOf": [
                {
                  "$ref": "#/$defs/EnvNames"
                },
                {
                  "type": "null"
                }
              ]
            },
            "params": {
              "type": [
                "object",
                "null"
              ],
              "additionalProperties": true
            },
            "pricing": {
              "description": "Rate override for this provider's model. See [`PricingCfg`].\n\nBoxed because `ProviderKind` is reachable from `AssertKind` (an\n`llm-rubric` can carry its own grader, which carries a provider), so\nfive inline `Option<f64>` here would inflate every assertion in\nevery suite by 80 bytes. Transparent to serde and schemars — the\nYAML is unchanged.",
              "anyOf": [
                {
                  "$ref": "#/$defs/PricingCfg"
                },
                {
                  "type": "null"
                }
              ]
            },
            "type": {
              "type": "string",
              "const": "anthropic"
            }
          },
          "required": [
            "type",
            "model"
          ]
        },
        {
          "description": "OpenAI-compatible chat-completions client.",
          "type": "object",
          "properties": {
            "model": {
              "type": "string"
            },
            "base_url": {
              "type": [
                "string",
                "null"
              ]
            },
            "api_key_env": {
              "anyOf": [
                {
                  "$ref": "#/$defs/EnvNames"
                },
                {
                  "type": "null"
                }
              ]
            },
            "params": {
              "type": [
                "object",
                "null"
              ],
              "additionalProperties": true
            },
            "pricing": {
              "description": "Rate override for this provider's model. See [`PricingCfg`].\n\nBoxed because `ProviderKind` is reachable from `AssertKind` (an\n`llm-rubric` can carry its own grader, which carries a provider), so\nfive inline `Option<f64>` here would inflate every assertion in\nevery suite by 80 bytes. Transparent to serde and schemars — the\nYAML is unchanged.",
              "anyOf": [
                {
                  "$ref": "#/$defs/PricingCfg"
                },
                {
                  "type": "null"
                }
              ]
            },
            "type": {
              "type": "string",
              "const": "openai"
            }
          },
          "required": [
            "type",
            "model"
          ]
        },
        {
          "description": "Arbitrary HTTP endpoint.",
          "type": "object",
          "properties": {
            "url": {
              "type": "string"
            },
            "method": {
              "anyOf": [
                {
                  "$ref": "#/$defs/HttpMethod"
                },
                {
                  "type": "null"
                }
              ]
            },
            "headers": {
              "type": "object",
              "additionalProperties": {
                "type": "string"
              }
            },
            "body": true,
            "output_expr": {
              "description": "minijinja expression selecting the output from the response.",
              "type": [
                "string",
                "null"
              ]
            },
            "type": {
              "type": "string",
              "const": "http"
            }
          },
          "required": [
            "type",
            "url"
          ]
        },
        {
          "description": "Embeddings endpoint used by the `similar` assertion.",
          "type": "object",
          "properties": {
            "model": {
              "type": "string"
            },
            "base_url": {
              "type": [
                "string",
                "null"
              ]
            },
            "api_key_env": {
              "anyOf": [
                {
                  "$ref": "#/$defs/EnvNames"
                },
                {
                  "type": "null"
                }
              ]
            },
            "params": {
              "type": [
                "object",
                "null"
              ],
              "additionalProperties": true
            },
            "pricing": {
              "description": "Rate override. Only `input_per_mtok` is read: an embedding call has\nno output tokens and reports no cache counters, so the other fields\nwould price components that do not exist.",
              "anyOf": [
                {
                  "$ref": "#/$defs/PricingCfg"
                },
                {
                  "type": "null"
                }
              ]
            },
            "type": {
              "type": "string",
              "const": "embeddings"
            }
          },
          "required": [
            "type",
            "model"
          ]
        }
      ]
    },
    "VerdictMode": {
      "description": "How an `llm-rubric` grader's structured verdict is obtained. `Forced` is\nthe default; `Auto` is documented but currently unread by the grader.",
      "type": "string",
      "enum": [
        "forced",
        "auto"
      ]
    },
    "TokenCount": {
      "description": "Which tokens a `tokens` assertion counts.",
      "oneOf": [
        {
          "description": "Input plus output. The default, and what every existing suite means.",
          "type": "string",
          "const": "total"
        },
        {
          "description": "Everything the provider bills for, cache reads and writes included. On a\ncache-heavy workload this can be several times `total`.",
          "type": "string",
          "const": "billable"
        }
      ]
    },
    "Defaults": {
      "description": "Values merged into every test before it runs.",
      "type": "object",
      "properties": {
        "vars": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/Val"
          },
          "default": {}
        },
        "assert": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Assert"
          },
          "default": []
        },
        "tags": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "threshold": {
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "cache_salt": {
          "description": "Suite-wide fallback for [`TestCase::cache_salt`], used only by cases that\ndo not set their own. A *constant* value here busts the whole suite on\nevery change — it is a fallback, not the granularity mechanism. Note it\ndoes not reach generator-produced cases (they are appended after the\ndefaults merge).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "Runner": {
      "type": "object",
      "properties": {
        "concurrency": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "retries": {
          "anyOf": [
            {
              "$ref": "#/$defs/RetryCfg"
            },
            {
              "type": "null"
            }
          ]
        },
        "rate_limit": {
          "anyOf": [
            {
              "$ref": "#/$defs/RateLimit"
            },
            {
              "type": "null"
            }
          ]
        },
        "timeout_ms": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "short_circuit": {
          "description": "Whether deterministic asserts short-circuit the grader. Default true.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "skip_on_empty_reason": {
          "description": "Empty reasons that mark a case `skip` instead of grading it.\n\nA blank output is a *successful* call, so it gets graded and scores zero\nagainst every assertion — for a reason that may have nothing to do with\nthe prompt. `skip` is the status for \"this cell was not gradeable, and\nthat is not a verdict about the prompt\": it is counted separately, and\ndoes not drag a pass rate down.\n\nOpt-in and empty by default, because which reasons qualify is genuinely\nsuite-specific and domarinn should not invent the policy. A refusal is\nusually a real result you want graded; `tool_use_only` against a harness\nthat declares no tools usually is not.\n\n```yaml\nrunner:\n  skip_on_empty_reason: [tool_use_only]\n```",
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "RetryCfg": {
      "type": "object",
      "properties": {
        "max": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "initial_ms": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "max_ms": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "jitter": {
          "description": "Spread concurrent retries so rate-limited cells do not all wake at once.",
          "type": "boolean",
          "default": false
        },
        "retry_after_max_ms": {
          "description": "Largest server-supplied `Retry-After` to honor, in milliseconds\n(default 120000).\n\nSeparate from `max_ms` on purpose: `max_ms` caps domarinn's own\nexponential growth, whereas `Retry-After` is a directive describing the\nserver's rate window. A hint above this ceiling errors the case rather\nthan retrying early, which would only earn another 429.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        }
      },
      "additionalProperties": false,
      "required": [
        "max"
      ]
    },
    "RateLimit": {
      "type": "object",
      "properties": {
        "rps": {
          "type": "number",
          "format": "double"
        }
      },
      "additionalProperties": false,
      "required": [
        "rps"
      ]
    },
    "CacheCfg": {
      "type": "object",
      "properties": {
        "backend": {
          "$ref": "#/$defs/CacheBackendKind",
          "default": "disk"
        },
        "s3": {
          "anyOf": [
            {
              "$ref": "#/$defs/S3Cfg"
            },
            {
              "type": "null"
            }
          ]
        },
        "grader": {
          "description": "Cache grader verdicts as well as provider responses. Default `true`.\n\nOn by default because it is the dominant recurring cost of an LLM-graded\nsuite: without it the judge is re-paid on every run even when every\nprovider response was a cache hit. Every way a verdict could go stale is\nin the key — the rubric, the grader's model and endpoint, its params, the\nsystem prompt, and the graded output itself.\n\nTurn it off to measure judge variance deliberately, or use\n`--no-grader-cache` for one run.",
          "type": "boolean",
          "default": true
        }
      },
      "additionalProperties": false
    },
    "CacheBackendKind": {
      "type": "string",
      "enum": [
        "disk",
        "layered",
        "http",
        "s3"
      ]
    },
    "S3Cfg": {
      "description": "Non-secret S3 settings; credentials come from the environment / AWS chain.",
      "type": "object",
      "properties": {
        "bucket": {
          "type": "string"
        },
        "endpoint": {
          "type": [
            "string",
            "null"
          ]
        },
        "region": {
          "type": [
            "string",
            "null"
          ]
        },
        "prefix": {
          "type": [
            "string",
            "null"
          ]
        },
        "force_path_style": {
          "type": "boolean",
          "default": false
        }
      },
      "additionalProperties": false,
      "required": [
        "bucket"
      ]
    }
  }
}
