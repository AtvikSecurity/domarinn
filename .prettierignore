# Generated files, verified byte-for-byte by CI drift checks — they MUST stay
# identical to the raw generator output, so no formatter may touch them:
# domarinn.schema.json by `mise run schema` (schema-check),
# web/src/api/generated by `mise run gen-types` (gen-types-check).
domarinn.schema.json
web/src/api/generated/

# Tool-managed files keep their tool's own formatting (pnpm rewrites both,
# and pnpm's quote style differs from oxfmt's — formatting them would make
# every install fight the pre-commit hook).
web/pnpm-lock.yaml
web/pnpm-workspace.yaml

# Build output.
target/
web/dist/
web/node_modules/

# Authored docs; leave their Markdown untouched by oxfmt.
docs/
